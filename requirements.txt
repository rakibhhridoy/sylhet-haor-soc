# Python dependencies for the Sylhet Haor SOC reproducibility package.
# Versions are those used to generate the published numbers (Python 3.11).
# Pinned for exact reproducibility; nearby versions should also work.
pandas==2.2.3
numpy==1.26.4
scipy==1.16.2
scikit-learn==1.8.0
geopandas==1.1.2
rasterio==1.4.4
pyarrow==23.0.1
matplotlib==3.10.9
openpyxl>=3.1          # read data/field/MainData.xlsx
# Optional -- only for re-running the Google Earth Engine extraction scripts (scripts/gee/):
# earthengine-api>=0.1.380
